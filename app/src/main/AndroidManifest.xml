<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.kaua.river">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.River">
        <activity android:name=".Activitys.ChatActivity"/>
        <activity android:name=".Activitys.CommonLayoutActivity" />
        <activity android:name=".Activitys.SignInActivity" />
        <activity android:name=".Activitys.ValidateEmailActivity" />
        <activity android:name=".Activitys.TermsAccountActivity" />
        <activity android:name=".Activitys.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter android:autoVerify="true">

                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" android:host="dev-river-api.herokuapp.com"/>
                <data android:scheme="https" />

            </intent-filter>
        </activity>
        <activity android:name=".Activitys.IntroActivity" />
        <activity android:name=".Activitys.SignUpActivity" />
        <activity android:name=".Activitys.MainActivity"/>
    </application>

</manifest>